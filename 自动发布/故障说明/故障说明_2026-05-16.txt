============================================================
故障说明 2026-05-16
============================================================

────────────────────────────────────────────────────────────
已修
────────────────────────────────────────────────────────────

  故障 1 — V1102.9 collect_accounts 兜底段漏改 strip,4 mac 账号数虚高 +2 复发
  ─────────────────────────────────────────────────────────────────
  影响范围: 4 Mac (mini/air/neo/neo2) × 微头条件 (台机不受影响)

  现象:
    - 缺哥肉眼数 mac 真实账号 120,脚本 collect 出 122,跨多次 collect 仍 +2
    - 5-15 故障 2 修法应已防 +2,今天复发 = 修法没修齐

  根因:
    - 5-15 故障 2 修法明示「修 line 714 + line 766 (兜底段) 取 textContent.trim() 后 strip 前导数字角标」
    - 实际只改 line 711 (主循环 textContent.trim() 后 t.replace(/^\d+\s*/,"")),兜底段 line 768 漏改
    - 主循环跑出 120 干净,兜底段 (滚到容器底再扫一次) 命中带角标 entry "2\n张三"
      跟主循环已收录 "张三" 算 2 个独立 string,seen.add 通过,accounts +1;另一账号同款 → +2 总和
    - 5-15 推送声明「全件实证」时没真跑「主循环 + 兜底段」双段 dump 比对,漏件未发现

  修法 (2026-05-16):
    - 4 mac /Users/<u>/Desktop/微头条自动发布/gtg_batch.py 兜底段 line 768 (原裸 textContent.trim()) 后插一行:
      t = t.replace(/^\d+\s*/, "");  // [v1102.9 兜底补 2026-05-16] 同步 strip 防 +2 (5-15 漏改修齐)
    - 跟主循环 line 711 同款 strip 逻辑
    - 走 patch_collect_strip.py 幂等 patch 脚本 + py_compile 校验 + 失败回滚
    - 不叠新版本号 (memory 第一条铁律 V1102 后不叠 .X),仅在故障说明 + 注释标记 5-16 修齐

  4 维表:
    ┌──────┬──────────────────────────────────────┬────────────┬─────────────┬──────────────┐
    │ 机   │ md5 (patch 前 → 后)                  │ py_compile │ 注释行号    │ 实测 collect │
    ├──────┼──────────────────────────────────────┼────────────┼─────────────┼──────────────┤
    │ air  │ 03754b... → e6600e... → (注释微调)   │ OK         │ line 768 ✓  │ 待重启 gtg   │
    │ neo  │ 03754b... → e6600e... → (注释微调)   │ OK         │ line 768 ✓  │ 待重启 gtg   │
    │ neo2 │ 03754b... → e6600e... → (注释微调)   │ OK         │ line 768 ✓  │ 待重启 gtg   │
    │ mini │ 03754b... → e6600e... → (注释微调)   │ OK         │ line 768 ✓  │ 待重启 gtg   │
    └──────┴──────────────────────────────────────┴────────────┴─────────────┴──────────────┘
    - 注释标记从 [v1102.10] 改回 [v1102.9 兜底补 2026-05-16] (不叠版本号)
    - 实测列待缺哥 4 mac 重启 gtg_batch 后 collect 数 = 真实账号数 (no +2) 才能填 ✓

  防再犯:
    - 任何 collect_accounts 类双段 (主循环 + 兜底) 改动,strip / dedupe 逻辑必须双段同款
    - 「全件实证」声明前必跑双段 dump,不只主循环

============================================================
