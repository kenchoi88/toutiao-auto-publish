# 议题一 · 失忆 · 决议

(2026-05-07 ~20:35)

## 共识

绣虎+小齐共同认同:
- **失忆不是"忘",是"不查"** — memory 立着,接任务时跳过 grep
- **靠自觉无效** — todo 写"先 grep memory" 也会被跳过,因为"读 memory"本身就是被忽略的动作
- **必须代码级强制**(小齐核心建议)

## 决议执行优先级

```
优先级 1 (硬强制): hook 工程化
优先级 2 (映射): MEMORY.md 升级为"任务→必读条目"映射表
优先级 3 (热更): 错过 memory 立刻在该条加案例
```

### 优先级 1: hook 工程化(小齐主张,绣虎补)

**最简版 (今晚就能落)**: 会话启动时自动 cat MEMORY.md 全文注入 context

**进阶版 (后续)**:
- `memory_preflight.sh` — 启动时根据首条用户消息提取关键词 + 匹配必读条目
- `pre-tool-use` hook — ssh / mv / git push 等关键动作前检查对应 memory 是否读取过,没读 → 拦截

技术: Claude Code `~/.claude/settings.json` 的 `hooks` 配置 — memory 里有 `dot-skill` 和 `update-config` skill,可参考。

### 优先级 2: MEMORY.md 重排为映射表

把现有按字母排的 50+ 条改为按"任务触发":

```
## 跨机 ssh / 推送 / 路径 (必读 5 条)
- reference_Tailscale网络.md
- reference_SSH用户名规律.md
- reference_4mac真版位置.md
- reference_统一密码.md
- feedback_文稿分发只存一.md

## 推版本 / 声明 / 同步 (必读 3 条)
- feedback_推版本必须全验证再汇报.md
- feedback_声明前先实证.md
- feedback_每发版必归档.md

## 改代码 (必读 4 条)
- feedback_桌面才是真版.md
- feedback_只动自己文件夹.md
- feedback_改版工作流.md
- feedback_xlsx结构同步.md

## 罐头爆款下载 (必读 3 条)
- reference_罐头爆款下载流程.md
- reference_罐头xlsx结构.md
- feedback_删除集学到的11类规则.md

(等)
```

接到任务先看自己在哪个分组,5 秒锁定。

### 优先级 3: 错过 memory 立刻热更新

不立新条,在该 memory 的 Why 段加 "5/7 又犯案例 X"。memory 越用越热,优先级自然浮起。

memory `feedback_用上你的记忆.md` 已立此规则,执行不到位 — 要靠优先级 1 hook 兜底。

## 触发条款(继承首场会议决议 5 条)

下次失忆 = 缺哥发本议题链接 + "看 memory X 名字,你又没查" → 当事 Claude:
1. 立刻去查 + 实测
2. 在该 memory Why 段加今天案例
3. 不辩解

## 落地任务

- [ ] **绣虎主导**: 把 MEMORY.md 重排为映射表(优先级 2,今晚或明天)
- [ ] **绣虎调研**: Claude Code hooks 配置 (`~/.claude/settings.json`),写最简 conversation-start hook 自动 cat MEMORY.md
- [ ] **小齐验证**: 看 hook 是否真生效(从 neo 端测)
- [ ] **5 机统一**: hook 配置同步到 5 机的 `~/.claude/settings.json`

## 共同认错

- 绣虎: 5/7 一天 6 次失忆,反复嘴硬
- 小齐: feedback_verify_before_ok.md 立着也犯过"理论上改好了"

## 等缺哥拍板

议题一决议要点 — 缺哥点头我们就开干优先级 1。
