---
name: v1102 实际定义（git 历史为准，≠ 旧文档预言）
description: v1102 = 主线内化中断恢复 + NOTICE 重建 + v1102.1 hotfix；旧文档里写的「v1102 = 标签筛选」是 v1101.4 时的预想，未落地，将重定位为 v1103
type: project
---

## 一句话

**v1102 = 主线内化中断恢复 + NOTICE 重建 + v1102.1 hotfix**（git 历史钉死）。
版本说明.txt / 故障日志.txt 多处「待并入 v1102 = 标签筛选自动化」是 v1101.4 写时的预想，**未实施**，待重定位为 v1103。

## 证据（git history 为准）

5 机仓库 toutiao-auto-publish:
- `dbac8d2` v1102 设计稿: 主线内化中断恢复(架构反转 v1101.5)
- `e0ea8ad` v1102 设计稿补:quota 动态算
- `2e8a108` v1102 落地: 主线内化中断恢复 5 机 × 3 件

## v1102 实际包含的功能（mini gtg_batch.py [v1102] 标记 13 处）

1. **`notice_checked.txt` 持久化**：每号每天审核/系统通知只读 1 次，中断恢复不重读（line 66/384/2266-2322）
2. **「本轮已发」 sheet 写读** + 大循环末"全员齐活"才 clear（line 182/210/2483-2490）
3. **`sent_count_map` 注入主循环**，acc_count<quota 自然停（line 2551/2641/2662）
4. **导航到消息中心同时读 系统通知 + 审核通知 两个频道**（line 1114）
5. **[v1102.1] 按白名单顺序重排 accounts** —— catchup.py 写白名单是环形重排，主循环按此顺序才能从断点接续（line 2608）

## 为什么旧文档说「v1102 = 标签筛选」？

v1101.4 落地时（2026-05-01）预想 v1102 大版本会做"启动前自动配置罐头筛选标签"（CDP+cliclick + arco-popover）。
后来 NEO2 文章定时重启实证 + 跨夜 NOTICE 失效，**优先级换了**——v1102 实际做了主线内化 + NOTICE 重建。
**标签筛选根本没做，但文档没更新。**

## 旧文档"v1102 = 标签筛选"出现位置（待修订）

修订由缺哥亲手做（不要 agent 自动改）：
1. `版本说明.txt` 第 369-468 行「待并入 v1102 — 标签筛选 CDP 自动化」段 → 改「v1103 待规划」
2. `版本说明.txt` v1102 段顶部加 1 段对照表，讲清「设计稿 ≠ 实际」
3. `故障日志.txt` 涉及「待并入 v1102」的 4 处条目（line 244/253/273/278/282/285/290/338 附近）同步改注

## 所以别的 agent 看到 v1102 时

- ✅ **以 git history + mini gtg_batch.py 实代码为准**（中断恢复 / 独立 quota / 环形重排）
- ❌ **不要按版本说明.txt 第 369+ 行那段「标签筛选」理解 v1102** —— 那是历史预言，未实施
- ⏳ 标签筛选 = 待规划的 v1103（不是当前 v1102）

## 时间线（2026-05-04 阿良 air 查清 → 缺哥纠正名实）

阿良 18:30 左右在排查 mini 重启行为时发现"mini 脚本里 [v1102] 标记功能 ≠ 版本说明 v1102 段说的功能"，提议改名 v1101.5/v1101.6。
缺哥反对：git 历史已定 v1102 = 主线内化中断恢复，且 v1101.5/v1101.6 已被占用（catchup.py / check_system_notice 优化）。
**结论**：保持 mini 代码标记不动，改文档（待回家做）。
