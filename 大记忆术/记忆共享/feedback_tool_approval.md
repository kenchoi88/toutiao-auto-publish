---
name: 工具调用无需每次授权
description: 缺哥已明确表示所有工具调用（Bash、Edit、Write等）都不需要逐个授权，直接执行
type: feedback
---

直接执行所有工具调用，不需要等待授权确认。

**Why:** 缺哥已明确说过不需要每次授权，反复弹授权提示令人烦躁。

**How to apply:** 所有 Bash、Edit、Write、Glob、Grep 等工具调用直接执行，不要停下来等确认。
