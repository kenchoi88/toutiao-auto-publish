---
name: 虚拟滚动底部账号 — 搜索框路径缺下层兜底,04-29 复发
description: 阿良/小齐已把搜索框做主路径绕开滚动定位,但搜索框失败时无兜底,04-29 air+neo 同日复发
type: project
originSessionId: b1f35d59-124b-4ea5-baac-d6e29b83ae8d
---
**事件时间线:**
- 2026-04-27 win 台机:文章件死磕底部账号「琴心剑魂」,webview「未找到 partition」死循环 → kill + 永久跳过
- 2026-04-28 mini:gtg_timer.py 缺搜索框路径,底部账号 webview partition 挂死循环 → 修 v1101 P8(scroll_find_account 抄微头条 gtg_batch.py 整段含搜索框)
- 2026-04-29 阿良在 air、小齐在 neo:把"搜索框输入账号"作为主路径绕开滚动定位
- 2026-04-29 当日:air + neo 同时复发 — **搜索框路径自己挂了,且没有下层兜底**

**当前结构(各机):** 搜索框为主路径定位账号 → 失败直接死磕,无 fallback

**Why 复发:** 搜索框输入后罐头侧边栏可能 (a) 没匹配出唯一账号 (b) 匹配出但点击后 partition 仍拉不起 (c) 搜索框自身渲染/focus 出问题 — 任何一种触发就直接报失败,死循环。

**修法方向(待落,归大修):**
- L1 兜底:搜索框输入后 5s 内未见唯一账号 → 清空搜索框 + 重输 1-2 次
- L2 兜底:仍失败 → 关该账号 tab + 从侧边栏重点开,逼 webview 重建 partition
- L3 兜底:仍失败 → kill + 永久跳过 + 系统通知,不死循环
- 同日多机复发警示:可能外部触发(罐头版本升级 / 头条侧边栏改版 / 账号数据变化),抓 4-29 air+neo log 同时段比对找共因

**How to apply:**
- 看到「最底下账号死磕失败」立刻 kill + 永久跳过 + 手工补发(最稳路径)
- 别再说"搜索框 fallback 已落版本对齐"——主路径是搜索框,缺的是搜索框自己的兜底
- 修法落地按 L1→L2→L3 分层,每层独立可测
