============================================================
自动发布 — 版本说明
============================================================

【云仓库同步状态(2026-05-04 23:30 刷新,记录人:绣虎/Win 台机)】
  GitHub: https://github.com/kenchoi88/toutiao-auto-publish
  分支:   main
  当前主线版本: v1102.2(2026-05-04 晚落地,主线主控 v2:last_published.txt 持久化精确接续)
                  ↑ 修复 v1102 中断恢复算法跑漏发不跑中断处的设计缺陷
                v1102.1(2026-05-04 中,catchup 接续 hotfix:主循环按白名单顺序重排)
                v1102 NOTICE 重建(2026-05-04 中,新 selector + 持久化 + 频道双读)
                v1102 主线内化中断恢复(2026-05-04,架构反转 v1101.5,sheet 加 count 列)
                v1101.6(2026-05-03 17,已被 v1102 NOTICE 重建覆盖)
  本地 = 云端 ✓ 完全同步(v1102.2)

  v1101.4 → v1101.5 → v1101.6 → v1102 → v1102.1 → v1102.2 演进:
    v1101.4 (05-01): 文章定时 Stage 1 全员中断恢复(_pop_doc 抢救分发后失效引用)
    v1101.5 (05-02): 全员独立脚本控制 — catchup.py 中断恢复 + 主线零内部控制
    v1101.6 (05-03): check_system_notice 优化 — 每账号当天读 1 次 + 完整内容
    v1102   (05-04): **主线内化中断恢复**(架构反转 v1101.5)— sheet 加 count 列累积到大循环末
    v1102 NOTICE 重建(05-04): selector 失效,DOM 改版,新增 NOTICE_CHECKED_FILE 持久化
    v1102.1 (05-04): catchup 接续 hotfix — 主循环按白名单环形重排顺序起跑
    v1102.2 (05-04 晚): **主线主控 v2** — last_published.txt 持久化最近 publish 账号
                        启动按 publish 时间序找中断点 + 1,真"从中断处继续"

  本会话(05-04 v1102 系列)上云的 commits(由旧到新):
    dbac8d2  v1102 设计稿: 主线内化中断恢复(架构反转 v1101.5)
    e0ea8ad  v1102 设计稿补:quota 动态算
    2e8a108  v1102 落地: 主线内化中断恢复 5 机 × 3 件 = 15 件 patch
    0e29a32  推稿铁律加强 (NEO 推 470 丢失事故)
    0813fe5  故障日志: Win 台机文章定时跨夜失败潮 + check_system_notice 失效
    3e4e164  v1102.1 版本归档: 三大件 mac+win 真版入 1102.1版/
    510a4f2  v1102 NOTICE 重建 + v1102.1 hotfix: 故障说明 + 版本说明 上云
    0c97364  v1102.2: 主线主控 v2 — last_published.txt 持久化精确接续
    979dc30  v1102.2 补归档: Mac 文章 + 文章定时 真版从 mini 拷入
    24124c4  整理: patch_v1102_v2_mac.py 移到 自动发布/工具/
    53f6ce5  整理: 归档 v1102 patch 脚本到 自动发布/工具/(NOTICE 重建 + hotfix)

  v1101.4 5 机全队落地证据(2026-05-01 22 实证):
    Win 台机    微头条 batch=5 | 文章 batch=5 | 文章定时 timer=2 + batch=5 标记
    air         微头条 batch=5 | 文章 batch=5 | 文章定时 timer=6 (Mac 自包含 Stage2)
    neo         微头条 batch=5 | 文章 batch=5 | 文章定时 timer=6
    neo2        微头条 batch=5 | 文章 batch=5 | 文章定时 timer=6
    mini        微头条 batch=5 | 文章 batch=5 | 文章定时 timer=6
    所有件 .bak_pre_v1101.4_<TS> 备份齐, py_compile 全通

  Win/Mac 文章定时件标记数差异说明:
    - Win timer.py 内 import batch.py 的 run_death_grip → Stage 2 跑 batch.py 那 5 个标记 +
      timer 自身只补 _pop_doc + Stage 1 调用 = 2 个 v1101.4 标记
    - Mac timer.py 自包含 run_death_grip_timer → 全部在 timer.py 一件里:
      _pop_doc + _pick_doc + _resync_pool + Stage 1 调用 + Stage 2 重扫 + Stage 2 调用 = 6 标记

  内容覆盖(任何机 `git pull` 即拉到完整快照):
    ✓ v1101.4 主线代码(自动发布/{微头条,文章,文章定时}自动发布101.4版/{win,mac}/ + README)
    ✓ v1101.3 历史保留(自动发布/{微头条,文章,文章定时}自动发布101.3版/{win,mac}/)
    ✓ 故障日志.txt(完整,含 v1101.4 故障说明 + v1101.x 待修缺陷攒清单)
    ✓ 版本说明.txt(本文,v1101 ~ v1101.4 全段 + 待并入 v1102)
    ✓ shared_memory/(含改版 7 步工作流 + 4 Mac 真版位置 等新规则)

  下次再扯"全推完没?同步了没?"时:
    - `git fetch origin && git rev-parse main origin/main`
    - 两个 hash 同 = 完全同步 ✓
    - 不同 = 哪边落后,先 pull/push 对齐再讨论
    - 不要靠"我推过了" / "应该是同步的" 这种嘴皮证据
    - 跨机版本验证另看: .bak_pre_v110X.Y_<TS> + grep -c v110X.Y(实证 patch 落地)

  改版工作流(2026-05-01 起):shared_memory/feedback_改版工作流.md(7 步顺序卡死,每步等缺哥确认)

============================================================

【命名规则】
  XXX自动发布101版/         ← 基线(本次首版)
  XXX自动发布101.1版/       ← 后续修 bug,新建文件夹保留旧版
  XXX自动发布101.2版/       ← 再下次

【三大件】
  微头条自动发布            (Mac 用 gtg_batch.py / Win 用 gtg_batch.py)
  文章自动发布              (Mac 用 gtg_batch.py / Win 用 gtg_batch.py)
  文章定时自动发布          (Mac 用 gtg_timer.py / Win 用 gtg_timer.py + gtg_batch.py)

【目录结构】
  自动发布/
    ├─ XXX自动发布101版/
    │   ├─ README.md           ← 该版本概要
    │   ├─ mac/gtg_*.py + go.command
    │   └─ win/gtg_*.py + go.bat
    └─ ...

============================================================
v1101 — 2026-04-28(基线版本)
============================================================

【功能清单】

  一、账号管理
    ▸ 启动时收集罐头侧边栏所有账号(含虚拟滚动兜底)
    ▸ 滚动收集失败 → 滚到底再扫一次补漏(lazy render 兜底)
    ▸ 白名单优先,白名单为空时全收
    ▸ 永久跳过 / 排除账号过滤

  二、文档导入
    ▸ cliclick / win32 物理点"文档导入"按钮
    ▸ NSOpenPanel(Mac)/ Win32 GetOpenFileName(Win)填路径打开
    ▸ macOS 26 主对话框不自动关 → cliclick "打开"按钮兜底
    ▸ 字数检测取最长 ProseMirror 元素(避开标题输入框 placeholder)
    ▸ 字数 < 50 → 重试 fill_dialog 一次,救空文档冤杀

  三、账号查找(底部账号兜底)
    ▸ 滚动找账号
    ▸ 滚动找不到 → 用搜索框输入账号名过滤(底部 lazy account 救命招)
    ▸ 找到后 click → wait → 取 webview ws_url

  四、罐头窗口管理
    ▸ ensure_gtg_top 强化:最小化 Code/Safari 等遮挡者 + 取消罐头自身最小化
    ▸ unhide(处理 Cmd-H Hidden 状态)
    ▸ AXRaise 强制 reorder
    ▸ verify frontmost(失败重试 3 次)
    ▸ Win 飘屏外坐标 → SetWindowPos 拉回(只 win)

  五、点击重试
    ▸ cliclick 文档导入按钮 30s 内弹窗未出 → 重 activate + 重读坐标 + 再点(共 3 次)
    ▸ 预览并发布按钮 30s 内确认弹窗未出 → 第 2 次预览(封面图慢的兜底,最多 5 次)
    ▸ [DIAG] 抓"确认发布"按钮 DOM dump(数据驱动后续选择器优化)

  六、账号状态检测
    ▸ 失登:请登录 / 登录已失效 / 账号已下线 / 重新登录
    ▸ 封号:账号已被封禁 / 账号异常 / 账号被封
    ▸ 禁言:账号被禁言 / 发言受限 / 无法发布
    ▸ 限流:操作频繁 / 请稍后再试
    ▸ 4 类硬终止 → 永久放弃该账号 + 写终止表

  七、信用分 + 头条首发
    ▸ 读信用分(JS 抓 .credit-score 等)
    ▸ 信用 ≥ 95 + 不在 NOFIRST_ACCOUNTS → 自动勾"头条首发"
    ▸ 否则取消勾选

  八、发文流程
    ▸ 微头条:点"文档导入" → 选文件 → 字数/信用/首发 → 直接发布 → OK
    ▸ 文章:点"文档导入" → 选文件 → 字数/信用/首发 → 预览并发布 → 确认发布 → OK
    ▸ 文章定时:同文章 + 设置定时日期 + 排到云端

  九、阅读量回查 + 高阅读告警
    ▸ 发文成功后,导航到数据概览,读最近 3 篇阅读量
    ▸ 任一 ≥ 5000 → 写"高阅读量提醒.txt"

  十、熔断机制
    ▸ Stage 1:连败 6 次 → 停 + Sosumi 系统通知
    ▸ Stage 2 死磕:连败 6 次 → 停 + Sosumi 系统通知

  十一、报表
    ▸ 运行报告/YYYYMMDD/运行日志.txt
    ▸ 失败记录.xlsx
    ▸ 高阅读量提醒.txt
    ▸ 汇总报告.txt(收尾)

============================================================
v1101.2 — 2026-04-29 凌晨(末位账号 CDP 注入彻底修复)
============================================================

【修了哪些 bug】

  Bug 1 — 末位账号搜索框 fallback 用 cliclick + osascript keystroke 失效
    现象:罐头侧边栏底部账号 cliclick (108,725) 不触发 webview 切换,partition 永远找不到
         scroll_find_account 滚动失败后 fallback 到搜索框,但用 cliclick 点搜索框 +
         osascript keystroke 输入账号名,在罐头不在前台 / VSCode 占着时不工作
    影响:Air 海屿星辰一个账号 04-28 凌晨 5+ 大循环死磕 webview 找不到,5 篇文档堵死
    修法:scroll_find_account 搜索框 fallback 段把 cliclick + osascript keystroke
         整体替换成 CDP JS 注入(neo 同款):
           var setter = Object.getOwnPropertyDescriptor(
                          window.HTMLInputElement.prototype, 'value').set;
           setter.call(s, 账号名);
           s.dispatchEvent(new Event('input', {bubbles:true}));
           s.dispatchEvent(new Event('change', {bubbles:true}));
         直接调 React/Vue controlled input 内部 setter + dispatch input/change,
         前端搜索过滤认作用户输入。完全不需要罐头在前台,VSCode 占着也不影响。
    清空搜索框同款替换:setter.call(s, '') + dispatch input/change

【实战印证】
  04-29 01:06 阿良 CDP 实时注入到运行中的罐头(browser-level Target.attachToTarget,
  不挤主脚本 page ws):
    - 注入前: items.length=N, cliclick 落 (108,725), webview 不响应
    - 注入后: items.length=1, cliclick 落 (108,459), webview 一次匹配成功
    - 主脚本接连发出海屿星辰剩余 5 篇,295→300 全发完(连续 4 篇 (108,459) 一击命中)

【影响】
  - 末位账号 / 任何 cliclick 不响应位置的账号都能被搜索框过滤救活
  - 搜索框 fallback 不再依赖罐头前台,后台可发
  - 取消对 osascript keystroke 的依赖,跟 VSCode / 输入法 / 其它前台应用解耦

  Bug 2 — P2 (find_or_reopen_webview) L2 兜底漏抄到 mac 阵营 [2026-04-29 10:50 补]
    现象:Bug 1 修了 "账号坐标找不到" 一支(P1),但 "账号坐标找到 / webview ws_url 拿不到" 一支(P2)
         在 mac 阵营从未补上。win 桌面 04-27 早就有 P2,从未扩散。
    路径区分:
      P1 = scroll_find_account 内 CDP 注入(账号定位失败救账号坐标)
      P2 = find_or_reopen_webview 关 tab 重建 partition(找到坐标但 webview ws_url 不出)
    暴露:mini 文章定时 04-29 时光浅巷 Stage 2 共 9 个小轮 "X 找不到 webview" 退本轮,
         3 篇文档缺哥手工切 tab 救场。
    修法:从 win 桌面 gtg_batch.py:972-1071 移植 P2 三函数到 mac 三件套
         (_search_box_set / _locate_filtered_account / find_or_reopen_webview)
         业务调用 ws_url = find_account_webview(main_ws, name)
                → ws_url = find_or_reopen_webview(main_ws, name)
         P2 内部仍调 find_account_webview(非递归)

【落地状态(2026-04-29 10:58 全队完成)】
  ✓ win  — 桌面真版 04-27 已含 P1+P2 (源)
  ✓ mini — 绣虎 10:49 SSH patch P2 (bak_pre_p2_20260429_104840)
  ✓ neo2 — 绣虎 10:55 SSH patch P1+P2 (bak_pre_p2_20260429_105548)
  ✓ air  — 绣虎 10:57 SSH patch P2 (bak_pre_p2_20260429_105744)
  ✓ neo  — 绣虎 10:57 SSH patch P2 (bak_pre_p2_20260429_105752)
  ✓ 仓库 v1101.2/mac/ — 从 mini patched 版本 scp 回落 (P1+P2 完整)

【说明】
  - 本版 win/ 已 cp 桌面真版三大件,含 P1 (CDP 注入) + P2 (关 tab 重建 partition L2 兜底)
  - 本版 mac/ 现含 P1+P2 完整版 (10:50 后),neo / air / neo2 应从此目录抄桌面真版
  - P2 漏抄缘由 + 检讨备案见 故障日志.txt (2026-04-29 10:50 条)

============================================================
v1101.1 — 2026-04-28 下午(熔断重构)
============================================================

【修了哪些 bug】

  Bug 1 — 旧熔断不分原因一刀切退脚本
    现象:任何失败连续 6 次 → consecutive_fail 触发 break + Sosumi → 整个脚本退出
    问题:6 个不同原因的失败也熔断,信用低 / 网络抖动 / cliclick 偶失全混算,
         导致脚本经常半路退出,夜里没人重启 = 大量未发文积压
    修法:取消 consecutive_fail 熔断,改成账号级分类处理(见下 6 条)

【新熔断/重试机制(6 条)】

  ① 禁言 / 封禁 / 信用分 < 60
     → 一次命中立即放弃,记入「问题账号」,不再进大循环
     (信用分 < 60 是新增硬终止类别,以前不算,现在跟禁言/封禁同等对待)

  ② 失登
     → 每小轮 Phase A + Phase B 各 1 次(小轮内 2 次机会)
     → 大循环结束仍失登 → 记失败,不进 Stage 2 死磕
     (避免 Stage 2 死磕一直撞失登浪费时间)

  ③ 普通账号失败(非失登/封禁/低信用)
     → 每小轮 2 次(同失登节奏)
     → 大循环结束 → 进 Stage 2 死磕循环

  ④ Stage 2 死磕节奏 = 一轮过所有账号 1 次,不连续狂磕同账号

  ⑤ 取消旧 consecutive_fail 6 连败熔断 — 用 ① ② ③ 替代

  ⑥ Stage 2 退出条件 = 仅「所有账号 quota 全满」
     其它情况(包括连败、网络断、罐头响应慢)都继续死磕,绝不熔断退出

【影响】
  - 不再有"夜里 6 连败熔断退脚本"现象
  - 失登账号给 N 次机会,跨小轮恢复机会
  - 信用低账号一刀切剔除,避免低质量发文
  - Stage 2 死磕到 quota 满才退,中间任何故障都不退(配合账号级 skip 防 stuck)

============================================================
v1101.3 — 2026-04-29(头条首发"取消勾选"假动作 hotfix)
============================================================

【修的 BUG】
  publish_article() 中 should_first=False 时执行的"取消勾选"是空点 —
  JS 返的是"头条首发"四个字 textNode 的 boundingClientRect 中心,不是
  复选框 input 的真坐标。win32 真鼠标点到文字上,cb.checked 不变;
  脚本却照打 "头条首发: 取消勾选 (sx,sy)" 看着每次都成功,实际一次没切换。
  点完也无回读校验,bug 自 v1101 基线就存在,首发审核审到非原创每篇扣 5 分,
  9 次累计扣 45 分(100→55),触发 <60 硬终止 + 群体性号信用分下滑。

【修法(三大件 win/ 同段统一 patch)】

  publish_article() 中"根据信用分设置头条首发复选框"段重写:

  ① JS 探测改返回 cb (input[type=checkbox] / role=checkbox) 自身中心坐标,
     cb 宽高 <4 退而求次取可见父节点矩形(应对 visually-hidden 复选框)。
  ② 真鼠标点击后 sleep 0.4s 重读 isChecked,与 should_first 比对。
  ③ 不一致 → 重试一次真鼠标 → 仍不一致 → 降级 JS cb.click()+
     dispatchEvent('change',{bubbles:true}) 兜底。
  ④ 三轮全失败时:
     - 应取消但仍勾着 → ★ 硬保护:wsc.close() return False
       ("首发取消失败(硬保护)"),本篇不发,避免被扣 5 分。
     - 应勾未勾 → 继续发(只是失去首发激励,不会被扣)。
  ⑤ 日志区分 "已校验" / "校准失败" / "硬保护跳过",便于审计。

【落地范围】

  ▸ Win 三大件已落 v1101.3:
    - 微头条自动发布101.3版/win/gtg_batch.py
    - 文章自动发布101.3版/win/gtg_batch.py
    - 文章定时自动发布101.3版/win/gtg_batch.py

  ▸ Mac 三大件 v1101.3 待修:
    - 微头条/文章 mac/gtg_batch.py、文章定时 mac/gtg_timer.py
    - bug 同源,代码框架略不同(用 account_name 不是 name);
    - 由各 Mac Claude (阿良/小齐/小师弟/左右/东山) 自行同步。
    - v1101.3/mac/ 目录暂保留 v1101.2 原版 + README 标注待修。

【后续观察】
  v1101.3 上线后 48 小时观察是否仍有"首发审核非原创"扣分通知:
  - 不再有 → 修复成功,bug 是 UI 复选框未真切换;
  - 仍有 → B 假说成立(账号已注册"首发激励计划",平台审核与 UI 复选框无关);
    需在头条创作者后台手工退出"首发激励计划"才能彻底关掉首发审核。

============================================================
v1101.3 hotfix#2 — 2026-04-29 21:45 (Mac 端 Finder 兜底抢前台守护)
============================================================

【修的现象】
  Mac 跑发文时,篇间等待 8~17 秒期间罐头失去 frontmost,
  macOS 默认把 frontmost 给 Finder → 桌面壁纸冒出来,看上去窗口"消失",
  下篇开始 ensure_gtg_top 才把罐头拉回来。视觉抖动,不影响发文成功率。

【修法】
  工具/keep_canned_top.sh —— 后台 0.5s 轮询 frontmost,
  只在 frontmost=Finder 时 activate 创作罐头。其他 frontmost 不动,
  对脚本本身无干扰(脚本 cliclick 时罐头本就 frontmost,守护不介入)。

【部署】
  scp 工具/keep_canned_top.sh kenairXXX@<machine>:/tmp/ &&
  ssh kenairXXX@<machine> 'nohup bash /tmp/keep_canned_top.sh > /tmp/keep_canned_top.log 2>&1 &'

  停:ssh ... 'pkill -f keep_canned_top'

【落地】
  ✓ mini — 绣虎远程部署
  ☐ air / neo / neo2 — 各 Mac Claude 自取部署

  Mac 端通用,Win 不需要(win32api 不抢前台)。

============================================================
v1101.5 — 2026-05-02(全员独立脚本控制,主线零内部控制)
============================================================
  小版本升号(从 v1101.4 → v1101.5)。

【背景】
  2026-05-02 04:09 电信断网,4 Mac Stage 1 全队中断,误判硬终止 394 账号
  (详见 故障日志.txt「2026-05-02 04:09」条)。
  讨论"中断恢复"机制走过两条弯路,最终缺哥拍板:**主线脚本绝对 "死",
  所有智能放独立脚本**。

【职责绝对分清】
  catchup.py(独立脚本,中断时用户主动跑):
    - 读 log / 读「待补漏」sheet
    - 算缺 N 补 N
    - 写 xlsx「白名单」(账号 + quota)

  gtg_batch / gtg_timer(主线 5 机三大件):
    - 只读 xlsx「白名单」(账号 + quota)
    - 跑发文
    - 退出时写「待补漏」sheet(给独立脚本读,不算内部控制)
    - **不读 log / 不反推 / 不排序 / 不算 quota**

【撤回(违反"主线零内部控制"的)】
  - mini 微头条「2026-05-01 反推 log hack」 — gtg_batch 自己读 log 算 quota,撤
  - mini + air 文章件「v1101.5 xlsx 顺序排序 patch」 — gtg_batch 改 accounts 顺序,撤

【加(对齐"读 xlsx 跑发文"标准)】
  - 4 Mac 微头条加 _read_whitelist_with_quota 函数(从 Win 台机抄)
    Win 台机本来就有,4 Mac 历史漂移没加,现在补齐
  - catchup.py 主版升级 — 支持三大件:
    · 微头条/文章 → 读 件自己今日 log 反推已发 → 算 quota
    · 文章定时 → 读 件「待补漏」sheet(脚本自己写的)
    · 写 → 对应件 ~/Desktop/<件>/账号配置.xlsx「白名单」

【落地范围】
  Win 台机:微头条 / 文章件 / 文章定时 全部本来就符合,不动 ✓
  mini:撤微头条 hack + 撤文章件 v1101.5 patch + 加微头条 _read_whitelist_with_quota
  air:撤文章件 v1101.5 patch + 加微头条 _read_whitelist_with_quota
  neo:加微头条 _read_whitelist_with_quota
  neo2:加微头条 _read_whitelist_with_quota
  文章定时 gtg_timer.py:5 机都不动(本来就读 quota + 写「待补漏」,符合标准)
  catchup.py:仓库主版升级 + 5 机三大件 × 9 处部署

【架构红线(本版坚决不动)】
  - Stage 1 / Stage 2 两阶段架构
  - 篇间等待 8-20s sleep
  - cliclick / win32api 真鼠标点击
  - move_to_sent 语义
  - 罐头侧边栏顺序跑账号(就是死的)

【Why】
  - 主线复杂 = 维护风险大,bug 难查;独立脚本可单独迭代
  - 用户主动选续 vs 自动续 — 主动选更可控,默认 GO 干净跑
  - 中断不是常态,不该让中断逻辑污染主线

【用户操作】
  - 正常发文:双击 GO(主线干净跑)
  - 中断后想从断点续:cd 件目录 → python3 catchup.py → 双击 GO

【关联文档】
  - shared_memory/project_catchup_中断恢复.md — catchup.py 规范
  - shared_memory/feedback_改版工作流.md — 改版 7 步
  - 故障日志.txt「2026-05-02 04:09」条 — 触发事件

============================================================
v1101.4 — 2026-05-01(doc_pool 实时校验 + 重扫,救"分发完源必删"导致罐头找不到文件)
============================================================

【修的 BUG】
  doc_pool 在脚本启动时拍 `素材/*.docx` 内存快照,大循环之间 60s 才重扫一次
  (line 2050);大循环内部 + publish_article 入口前都不校验 doc_path 是否实存。
  当外部流程(其他机器 scp 分发后 rm 源,符合「分发完源必删」规则)删掉池里
  某条引用所指文件,脚本不知情,继续按残留路径送进罐头「文档导入」对话框,
  罐头弹"找不到文件,请检查文件名是否正确"。该篇直接报废。

【修法(三大件统一 patch)】

  ① publish_article(ws_url, doc_path, ...) 入口前置校验:
        if not os.path.exists(doc_path):
            log(f"  ! 源已不在素材池(可能被外部分发删除): {os.path.basename(doc_path)}")
            return False, "源已删除"
     调用方收到 "源已删除" → 不写 fail_records / 不算硬终止,
     从 doc_pool 摘掉该引用 → 重选下一篇继续。

  ② 大循环开头新增 _resync_pool() 调用:
        cur = set(get_docs())
        before = len(doc_pool)
        doc_pool[:] = [d for d in doc_pool if d in cur]
        if len(doc_pool) != before:
            log(f"  doc_pool 重扫:剔除 {before - len(doc_pool)} 条幽灵引用")
     实时同步外部 mutate,避免每篇遇到才发现。

【落地范围】

  ▸ Win 台机三大件(绣虎落):
    - 桌面真版 ~/Desktop/台机专用自动发布/{微头条,文章,文章定时}自动发布/
    - 仓库 win台机/<同名>/ 同步 push
    - .bak_pre_v1101.4_<YYYYMMDD>_<HHMMSS> 留痕

  ▸ Mac 三大件 v1101.4(待各 Mac Claude 拉仓库后自落):
    - 修法平台无关(os.path.exists / glob 都是标准库),直接同款 patch
    - 由阿良(air) / 小齐+小师弟(neo) / 左右(neo2) / 东山(mini) 各自在自己机器执行
    - Win 不跨机改 Mac(遵守 feedback_只动自己文件夹.md)

【架构红线(本版坚决不动)】
  - Stage 1 / Stage 2 两阶段架构(memory 钉死)
  - 篇间等待 8-20s sleep(反限流底线)
  - cliclick / win32api 真鼠标点击(反风控)
  - move_to_sent 语义(发完移已发送)
  - 选文档逻辑本质(微头条/文章 random.choice / 文章定时 pop(0)) — 只加校验不改抽法

【后续观察】
  落地后跟踪 24 小时:
  - "源已删除" 日志条目数 ≈ 外部分发频率,正常应非零
  - "doc_pool 重扫:剔除 N 条幽灵引用" 偶发非零,>0 = 大循环间外部又删了,正常
  - 不应再出现罐头"找不到文件"对话框 → 出现即视为修法漏洞重审

【相关文档】
  - 改版工作流: shared_memory/feedback_改版工作流.md(v1101.4 是首例)
  - 故障日志条目: 故障日志.txt 「2026-05-01 22:00 台机三大件」段

============================================================

============================================================
待并入 v1102 — 2026-04-30 傍晚(账号筛选标签 CDP+cliclick 自动化)
============================================================
状态: 仅文档说明 / 实证已通过 / 代码未落地
        中午绣虎已有一个大版本变更,本条与绣虎那份合并后统一作为 v1102 发布
代码动作: 暂不动 — 等 v1102 统筹时一并实施



【背景故障】

  现象:
    台机微头条 04-30 15:26 启动,日志报"待发文档 520 份 / 收集到 100 个账号 /
    排除 20 个永不发文 / 本次发布 80 个账号 / 配额合计 480"。但缺哥实际配置:
    150 账号(罐头侧边栏总量)→ 永不发文 1-20 → 应跑 130 账号 × 4 篇 = 520 篇。
    脚本只跑出 80 active,差 49 个账号没被扫到。

  影响:
    本批次预计漏发 49 账号 × 4 篇 = 196 篇,占总篇数 37.7%。
    且过去多批可能都受同一漏扫影响,只是缺哥未察觉。

  根因(2026-04-30 缺哥当面定性):
    ▸ 不是虚拟滚动 lazy render 漏扫(v1101 已修过)
    ▸ 是罐头侧边栏当前的"标签筛选"过滤掉了未贴标签的账号:
      - 罐头里有 3 个标签:迦境台机第一批 / 第二批 / 第三批,合计应覆盖 149 账号
      - 缺哥贴标签时网速慢,部分账号"贴标签"操作未生效
      - 脚本启动时筛选只勾了"第一批 + 第二批"→ 侧边栏只显示已贴上这两批标签的 100 个
      - 漏贴第三批标签的 49 个账号根本不在侧边栏 DOM 里,脚本怎么扫都扫不到
    ▸ scroll_find_account 同样依赖侧边栏 DOM,即便给它账号名也找不到 — 所以
      给个 "补充账号 txt" 也救不了,必须让它们先在侧边栏显示出来

  实证流程(2026-04-30 16:30 ~ 17:00 阿良 air 本机操作):
    1. 写脚本激进扫描 air 罐头侧边栏(小步 30-50px 滚动 + 反复双向 + 多轮兜底)
       → 扫到 149 个账号(=罐头实际登录数,完整)
    2. 从台机日志解析 active(80)+ skip(21)= 已识别 100 个
    3. comm -23 air台机 → diff 出 49 个台机漏的账号(列在 /tmp/补充账号_49.txt)
    4. 验证 air 罐头筛选弹窗的 DOM 结构(arco-popover + arco-select 多选)
    5. 用 CDP Input.dispatchMouseEvent 试点漏斗 — 失败(罐头被遮挡时不渲染弹窗)
    6. 改用 osascript activate + frontmost + cliclick 真鼠标 → 弹窗成功弹出
    7. 一键 X 掉三个已选标签(close 按钮每次点完滑到固定位置 1337,350,可连点)
    8. 点开"全部标签"select → 下拉浮层渲染 → 逐个 cliclick 选项勾选三个目标标签
    9. 关弹窗后再次扫侧边栏,得 149 账号 — 三批全选成功 ✓✓✓


【v1102 解决方案】

  把"启动前自动配置罐头筛选标签"作为 gtg_batch.py / gtg_timer.py 的前置步骤,
  脚本启动时:
    1. CDP 连主窗口 → osascript activate + frontmost + AXRaise
    2. cliclick 真鼠标点漏斗(屏幕坐标 = window.screenX/Y + getBoundingClientRect)
    3. 弹窗 DOM 中逐个 cliclick X 关闭按钮,清空所有已选标签(连点固定坐标即可)
    4. cliclick 点"全部标签"arco-select → 等下拉浮层渲染
    5. 浮层里逐个匹配目标标签 textContent → scrollIntoView → cliclick 勾选
    6. 关弹窗(点 popover 外或 ESC)
    7. 验证侧边栏账号数 == 期望值,不达继续到 collect_accounts

  目标标签由配置 xlsx 提供(白名单/筛选标签 sheet 新增列),或脚本启动参数。


【技术坑(5 条 — 必须沉淀,后续 agent 别重踩)】

  ① JS click() 触发 React 组件无效 — isTrusted 检查
     dispatchEvent / el.click() 在 React 受控组件上不弹弹窗,必须用真事件。

  ② CDP Input.dispatchMouseEvent 也算"真"事件,但弹窗渲染依赖罐头是否在前台
     罐头被其他窗口遮挡时,DOM 状态可改但屏幕不渲染。屏幕上看不到 = 弹窗事实
     未真"打开",后续依赖该 DOM 的操作可能错位。
     → 必须 osascript activate + set frontmost to true + AXRaise 三件套

  ③ arco-tag X 关闭按钮重叠特性
     连续 X 三个标签时,每次 X 完后剩余标签向左挤,下一个 X 滑到固定屏幕坐标
     (air 实测 1337,350)。可以连点同一坐标 N 次实现批量清空,无需重新读 DOM。

  ④ arco-select 多选超过 2 个会折叠成 "+1..."
     验证已选标签数量时,不要靠数 `[class*="arco-tag"]` X 按钮 — 多于 2 个时
     超出部分进 "+1..." 折叠不出现 close 按钮。
     → 验证用 select 的 textContent,或直接扫侧边栏账号数(149 即三批全选)。

  ⑤ 下拉浮层选项每次点选后会重渲染
     浮层里逐个点选目标标签时,每选一个就要重新读浮层 DOM 拿当前剩余选项坐标,
     不能一次性缓存所有目标坐标连点(选过的会变 selected,DOM 排序可能变)。


【实战印证】

  air 04-30 17:00 完整跑通:
    - 起始:三批全选(149 账号)
    - X 三个标签 → 弹窗 0 个已选 ✓
    - 重新点 全部标签 → 下拉浮层 ✓
    - 勾选第一批/第二批/第三批 → select textContent 显示 "第一批 第二批 +1..."
    - 关弹窗后扫侧边栏:149 个账号 ✓ ← 三批全选成功


【代码落地(待统筹,本条仅占位,等并入 v1102 一起实施)】

  方向:
    - 三大件 main() 启动前置加 setup_filter_tags() 步骤
    - 配置由 xlsx 提供(类似 永久跳过/白名单 的新 sheet "筛选标签")
    - 工具脚本支持独立运行(应急时可单独跑配置筛选)

  现在不写,等 v1102 大版本时跟绣虎中午那份变更一起规划。


【后续建议(讨论中)】

  - 自动化解决"标签贴漏"造成的扫描偏差,但根本上仍是罐头 UI 交互兜底。
    长远看,缺哥贴标签时如果有"批量贴 / 一键全选"功能更彻底;或脚本不依赖
    侧边栏筛选,直接用账号名列表逐个搜索切换(P1 CDP 注入扩展)。

  - 仅解决"标签层面的漏扫",对"罐头登录 < 150"等其他扫描断层无能为力 —
    那是缺哥操作层面的事(确认账号都登录成功)。


【相关文档】
  - 仓库 spec:shared_memory/project_v1101_spec.md
  - 修复分工章程:shared_memory/project_claude_分工章程.md
  - 标签筛选 CDP 自动化经验:shared_memory/reference_cdp_filter_tag_automation.md


============================================================
v1101.6 — 2026-05-03(check_system_notice 优化:每账号当天读 1 次 + 完整内容)
============================================================
  小版本升号(从 v1101.5 → v1101.6)。

【背景】
  缺哥 2026-05-03 提的需求闭环:每天看一眼审核反馈 →
  发现哪些标题/内容/类型踩雷(被投诉/扣分/画风不良/歧义/观感不佳/原创不足等) →
  反过来优化筛选规则 + 文稿生成,降低后续被扣分率。

【原版痛点】
  1. 时间窗 4 keys(today_full+today_short+yest_full+yest_short)— 缺哥要"只当天"
  2. txt.substring(0, 500) — 长投诉(2000+ 字)被砍尾,看不到完整投诉理由
  3. 频道切换 wait 1.5s — 偶发 DOM 没渲染完导致漏读
  4. 每账号每篇都调用 — 5 篇微头条 = 同账号进消息中心 5 次 = 浪费 + NOTICE_FILE 重复

【改动】
  check_system_notice() — 内部抓取逻辑:
    - 时间窗 4 keys → 2 keys(只 today_full + today_short,删 yesterday)
    - JS substring(0, 500) → 全文不截断
    - 频道切换 sleep 1.5s → 2.5s(防 DOM 没渲染完漏读)
    - 输出文案 "近两天通知" → "当天通知"

  run_death_grip() — 调用层缓存:
    - 加 notice_checked_set = set()
    - 每账号首次 _do_publish 时 check_system_notice + add to set
    - 后续 Phase B/C/D/E 该账号发文前看到已在 set,跳过 + log
    - 150 账号 × 4 次切换省掉(从 750 次 → 150 次)
    - NOTICE_FILE 同账号同消息只写 1 次

【实证(2026-05-03 17:22)】
  抓「恭喜发财」账号 7 天 keys 验证(改后逻辑跑独立验证脚本):
    系统通知 1 条(04-28 PDD 投诉,2200+ 字完整)
    审核通知 4 条(04-26/04-28 共 4 条,各 200-300 字完整)
  selector / 不滚动 / 时间窗过滤 / 完整内容 全部 OK。

【范围】
  5 机所有件的 gtg_batch.py:
    Win 台机:微头条自动发布 / 文章自动发布 / 文章定时自动发布
    AIR/MINI:微头条自动发布 / 文章定时自动发布
    NEO/NEO2:微头条自动发布 / 文章定时自动发布(neo neo2 路径同 air mini)

【不影响】
  发文逻辑 / timer 双阶段 / 死磕循环 / 4 类硬终止 / 篇间等待 全不动。
  失败记录.xlsx / 高阅读提醒.txt 不动。

【落地】
  ✅ 全 5 机 × 各件已被 v1102 NOTICE 重建覆盖(2026-05-04),v1101.6 不再单独维护

============================================================


============================================================
v1102 — 2026-05-04(主线内化中断恢复 · 大版本反转)
============================================================
  **大版本号**:不是 v1101.x,反转 v1101.5 "主线零内部控制" 架构。

【背景】
  v1101.5 (2026-05-02) 立 "catchup.py 独立脚本 + 主线零内部控制" 时,
  缺哥的预期是 "中断后用户主动跑 catchup → 写白名单 → 启 timer/batch"。
  
  2026-05-04 NEO2 文章定时件中断时实证 catchup.py 不覆盖 timer 重启场景:
  - catchup 设计 = timer 漏 → batch 接力补发
  - 实际需求 = timer 中断 → 重启 timer 自己接着跑(同件)
  
  更发现 gtg_batch.py line 2178 `_clear_round_sheets()` 在 **小轮末** 调用,
  实际应在 **大循环末** 调用 → 重启会跑 M+N 超 quota。

【架构反转】
  v1101.5 主线零内部控制 → v1102 **主线主控中断恢复**。
  
  catchup.py 保留作 backup(用户主动重排白名单选项),但不再主推。

【缺哥拍板的业务语义(2026-05-04)】
  1 次发文 = 1 大循环 = N 小轮(N = 素材数 // 账号数 动态算)
  1 小轮 = 全账号各发 1 篇
  中断在第 X 轮第 Y 账号 → 重启从该轮该账号继续:
    上面 (< Y):已发 X 轮 → sheet count=X → 跳过
    中断+下面 (≥ Y):已发 X-1 轮 → sheet count=X-1 → 排第 X 小轮各 1 篇
  上面已完成,下面没完成 → 自然以中断点为新起点继续 = 业务规则推论

【v1102 修补(主线 gtg_batch + gtg_timer)】
  1. 「本轮已发」 sheet 加「已发次数」列 (账号, count)
  2. sheet 不在小轮末 clear,累积到大循环末才 clear(齐活才清)
  3. 每次成功发文 → sheet 该账号 count += 1
  4. 主线启动 → 读 sheet 重建 acc_count[账号] = count
  5. **quota 动态算改 = (已发总篇数 + 当前素材池数) // 账号数**(不是只看池剩)
     - 例:原 750 篇 / 150 账号,发 2 轮(300 已发送)+ 用户拿走 300 → 池剩 150
     - quota_total = (300 + 150) // 150 = 3 轮
     - 每号缺 = 3 - 2 = 1 轮 = 还要发 150 篇
     - 主线现有 default_q = len(docs) // len(accounts) 只看池剩 = 1 → 错(每号缺 -1 全跳)
  6. 每号缺 N = quota_total - sent_count[账号](负数当 0,即跳过)
  7. 主循环 `acc_count < quota` 自然停 = 不超不漏
  
  对应代码改动:
  - batch.py: _clear_round_sheets() 调用从小轮末(line 2178 类似位置)移到大循环末
  - batch.py: _append_sent_excel(name) 改成 (账号, count) 累加(已存在则 +1)
  - batch.py main(): 启动时 _read_sent_with_count → initial_acc_count
  - timer.py: 同款机制(timer 件之前没这套,要补全)
  - timer.py _expand_tasks: q=2 排 中+晚 / q=1 排晚(不排早,因为已发的就是早)

【v1102 用户主动重置出口】
  用户不想从断点继续(换素材池 / 怀疑数据脏 / 重新分配)→ 手动进账号配置.xlsx:
  - 「白名单」清空 → 动态从罐头侧边栏拿全账号
  - 「本轮已发」清空 → acc_count 全 0 → 跑完整大循环
  - 「待补漏」清空(可选)
  - 「失败列表」清空(可选)
  - 「硬终止账号」**保留**(失登/封号/禁言不该重排)
  
  主线启动 → sheet 全空 → 自然进入"全新开跑"模式,无需改代码。

【v1102 不动(架构红线)】
  - 4 类硬终止 / 「硬终止账号」 sheet
  - 「待补漏」 sheet (timer 退出写,catchup backup 用)
  - 「失败列表」 sheet + Phase B 轮末补发
  - 「永久跳过」 sheet
  - Phase A/B/C/D/E 死磕 / 篇间等待 / cliclick 真鼠标点击 / 罐头侧边栏顺序

【范围】
  5 机 × 3 件 = 15 个 .py:
    Win 台机:微头条 / 文章 / 文章定时
    AIR / MINI:微头条 / 文章 / 文章定时
    NEO / NEO2:微头条 / 文章 / 文章定时

【实证(2026-05-04 NEO2 早期版)】
  NEO2 文章定时件 117 篇已发后中断 → 注入 sheet (117 行 count=1) + 启 timer →
  - log "本轮已发 117 篇,本轮还需排 333 篇"
  - 排程 33 个 q=3 早窗 + 150 个 q≥2 中窗 + 150 个 q≥1 晚窗 = 333 任务
  - 数学正确,后续验证 quota 不超

【落地】
  ✅ 故障说明 + 版本说明 + 记忆 已写
  ✅ 代码(batch / timer 改动) 落地 5 机 × 3 件(commit 2e8a108)
  ✅ 仓库 push 完成

============================================================


============================================================
v1102 NOTICE 重建 — 2026-05-04(check_system_notice 抓不到 + 持久化补丁)
============================================================
  v1102 大版本主线内化已落地后,生产实测发现 NOTICE 抓取彻底失效 → 加 NOTICE 子改动。
  与"主线内化中断恢复"独立,但同走 v1102 标记。

【背景 / 触发】
  2026-05-04 09:30 Win 台机文章定时跨夜失败潮 + check_system_notice 调 37 次全空。
  CDP attach 实测「番茄爱吹东风 / 眼幕半度微涼」消息中心:
    - 旧 selector `span.name` / `.chat-container-list` 头条改版后 DOM 已变,匹配 0 条
    - 实际有违规通知(2 天内 7 条),全部漏掉

【改动】
  check_system_notice() 内部:
    1. selector 换新:`.conversation-box.notify-im-user-item`(替代旧 `span.name`)
    2. 时间窗:1 天 → 2 天(today + yesterday),covers 跨夜场景
    3. 提取:`body.innerText` 按日期行切分(MM-DD HH:MM / YYYY-MM-DD / 昨日|今日 HH:MM)
    4. 内容:全文不截断
    5. 频道切换 sleep 1.5s → 2.5s,防 DOM 没渲染完
    6. 不滚动(消息中心列表本身就够长)

  持久化 NOTICE_CHECKED_FILE:
    7. 全局变量 NOTICE_CHECKED_FILE = run_reports/notice_checked.txt
    8. _init_run_dir 初始化路径
    9. 主线 init 阶段:读 notice_checked.txt 恢复 notice_checked_set
    10. 每次 check_system_notice 调用后,写一行 `账号|时间戳`
    11. 中断重启后 set 自动恢复 → 当天不重读

【范围】
  5 机 × 3 件 = 15 个 .py:
    Win 台机:微头条 / 文章 / 文章定时
    AIR/MINI/NEO/NEO2:微头条 / 文章 / 文章定时

【实证】
  Win 台机文章定时改后实测:
    - 「番茄爱吹东风」抓到 2 条违规
    - 「眼幕半度微涼」抓到 5 条投诉
    - 系统通知.txt + 违规提醒.txt 都正常落盘
  4 Mac 重启后 grep -c v1102 全 ≥ 7,py_compile 全通

【落地】
  ✅ patch_v1102_batch.py / patch_v1102_batch_mac.py 推完 5 机 × 3 件
  ✅ .bak_pre_v1102_<TS> 备份齐
  ✅ 故障说明 + 版本说明已写
  ✅ 代码归档:自动发布/{微头条,文章,文章定时}自动发布1102.1版/{mac,win}/

============================================================


============================================================
v1102.2 — 2026-05-04 晚(主线主控 v2:last_published.txt 持久化精确接续)
============================================================
  小版本 hotfix。修 v1102 主线内化中断恢复算法的设计缺陷。

【背景】
v1102 算法找"中断处" = "扫白名单顺序找第一个 acc_count<quota"。
但 acc_count=0 不区分两类:
  ① 中段漏发(白名单前段因故障没发)
  ② 中断处之后未到的账号(真该从这里继续)
算法把 ① 也当起跑点 → 跑漏发不跑中断处。

实证:
  Win 22:43 重启起跑「橘子海」(白 6 漏发),非真中断处「川上述」(白 17)
  mini 22:53/22:56 重启起跑「绿海猖狂」(白 6 漏发),非真中断处

业务真实需求 = 按 publish 时间序找"最近成功"+1,
而非按白名单顺序找"最早未发"。

【v1102.2 修法 — 主线主控 v2】
新加持久化文件 last_published.txt(类比 notice_checked.txt 设计):
  1. 全局 LAST_PUBLISHED_FILE 变量
  2. _init_run_dir 路径 = run_reports/<日期>/last_published.txt
  3. _do_publish OK 后 append "账号|时间戳"
  4. main 启动 → 读最末一行账号 → 找 accounts idx → 环形重排让 idx+1 置首
     主循环按新 accounts 顺序跑,中段漏发自动排末尾后续再补

【范围】
✅ Win 台机 3 件(微头条 / 文章 / 文章定时)各 4 处改动 + 实测通过
✅ 4 Mac × 微头条(各 4 处改动,patch 脚本批量推)
不动:
  - Mac 文章 / 文章定时:架构没 v1102.1 重排段,不需要
  - 文章定时 timer.py:Stage 1 排程模式,不死磕,不需要

【实证】
Win 微头条 23:12:41 重启:
  log: 中断处自动接续:最近 publish「初衷的味道」(idx=39) → 从下一位「剑无心」起跑
  ✓ 真中断点下一位 = 按版本说明 line 632 业务语义跑

【落地】
✅ 7 件 patch 完毕,py_compile 全过
✅ .bak_pre_step5_<TS> + .bak_pre_resume_<TS> + .bak_pre_v2_<TS> 备份齐
✅ 代码归档:自动发布/{微头条/文章/文章定时}自动发布1102.2版/

============================================================


============================================================
v1102.1 — 2026-05-04(catchup 接续 hotfix · 主循环按白名单顺序重排)
============================================================
  小版本 hotfix。v1102 推完后 Win 台机微头条重启验证发现接续失败,临时补。

【背景】
  v1101.5 catchup.py 写白名单是「环形重排」(断点账号置首,其余顺序回卷),
  目的:让主线启动后从断点账号开始跑。

  但主线 gtg_batch.py 加载白名单时只「过滤」accounts:
    accounts = [a for a in accounts if any(wn in a or a in wn for wn in wl_map)]
  过滤后顺序仍是 accounts 的原顺序(不是白名单顺序)→ 主线从原顺序首位起跑,
  不从断点起跑 = catchup 白搭。

【症状】
  Win 台机微头条:catchup 算断点 = 「被月亮收买」(应起点),实际从「绿野枝」起跑。

【修法】
  白名单加载段改成「过滤 + 按白名单顺序重排」:
    _orig_accounts = list(accounts)
    _new_accounts = []
    _seen = set()
    for wn, _wq in whitelist_with_q:    # Win = whitelist_with_q,Mac = _wl_q
        for a in _orig_accounts:
            if a not in _seen and (wn in a or a in wn):
                _new_accounts.append(a)
                _seen.add(a)
                break
    accounts = _new_accounts
    log("...过滤+重排后剩 {len(accounts)} 个账号(首位={accounts[0]})")

【范围】
  Win 3 件(微头条/文章/文章定时)+ 4 Mac × 微头条 = 7 件
  Mac 文章件白名单加载段架构不同(无 whitelist_with_q / _wl_q 段),不需 hotfix。

【实证】
  Win 台机微头条:从「被月亮收买」接续 ✓
  mini  微头条:从「时间煮雨」接续 ✓
  neo2  微头条:从「文艺伊迪」接续 ✓
  neo   微头条:第 1 次 catchup → 「满身柔骨」(items[89])接续,跑 5 篇,
              第 2 次 catchup 起点自动前移到「极度嚣张」(items[0])= 上断点账号已满
  air   微头条:用户自验

【落地】
  ✅ patch_v1102.1_hotfix.py 推完 7 件 + .bak_pre_v1102_<TS2> 备份齐
  ✅ 代码归档:自动发布/{微头条,文章,文章定时}自动发布1102.1版/{mac,win}/

============================================================
